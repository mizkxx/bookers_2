<%= render 'books/list', books: @user_books, user: @user, book:@book %>

<div class="container"> <!-- ここを追加 -->
<div class="row">   <!-- ここを追加 -->
<div class="col-lg-8">

<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>
<h2>Book Detail</h2>
<table class='table table-hover table-inverse'>
	<thead>
		<tr>
      <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
    </tr>
	</thead>

  <tbody>
  <tr>
  <% @user = @book_show.user %>
    <td><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %>
    <br><%= link_to(@user.name, "/users/#{@user.id}") %></td>
    <td><%= link_to(@book_show.title, "/books/#{@book_show.id}") %></td>
    <td><%= @book_show.body %></td>
  

<% if @book_show.user == current_user %>
  <td><button type="button" class="btn btn-success btm-sm"><%= link_to 'Edit', edit_book_path(@book_show), class:'edit_#{book.id}' %></button></td>
  <td><button type="button" class="btn btn-danger"><%= link_to "Destroy", book_path(@book_show), method: :delete %></button></td>
<% end %>

</tr>
  </tbody>
</table>

<%= link_to "back", books_path, class: "back" %>
</div>
</div>
</div>